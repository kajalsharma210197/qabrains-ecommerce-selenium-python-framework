[pytest]
testpaths = tests
pythonpath = .
addopts =
    -v
    --alluredir=reports/allure-results
    --clean-alluredir
markers =
    smoke: Login smoke tests
    cart: Cart workflow tests
log_cli = true
log_cli_level = INFO
